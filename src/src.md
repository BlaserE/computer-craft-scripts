# Source File
The source file (`src`) is where everything that is meant to be compiled into a single executable or does not run by itself goes.
It unclutters the repository's front page and remains clean.
